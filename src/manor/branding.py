"""表示名（主人に見える名前）を1か所で決める。

**内部の識別子とは別物である。** コマンド名・パッケージ名・DB・設定ファイルの場所・
localStorage の鍵はすべて ``manor`` のまま——これらは主人の環境に既に書き込まれていて、
名前を変えると動いているものが壊れる。ここで決めるのは**画面と文章に出る名前**だけ。

主人の指定（2026-09-05）: 「名前は "AI Manor" とします」。
"""

from __future__ import annotations

#: 画面・文章に出す名前。ここだけ書き換えれば全体に効く。
APP_NAME = "AI Manor"

#: コマンド・パッケージ・設定の識別子。**表示名を変えてもここは変えない。**
CLI_NAME = "manor"
