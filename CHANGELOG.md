# CHANGELOG — manor の変更履歴

執事が**自分の仕組みを変えたとき**の記録。**何を・なぜ・どう影響するか**を機能単位で残す。

## 他の記録との住み分け

| ファイル | 書くもの | 読む場面 |
|----------|----------|----------|
| **CHANGELOG.md**（ここ） | **執事の能力がどう変わったか**。機能単位・時系列 | 「いつ何ができるようになったか」を辿るとき |
| `butler/GROWTH.md` | **なぜそう変えたか**の学び（観測→仮説→打ち手→検証） | 同じ失敗を繰り返していないか確かめるとき |
| `home/LOG.md` | 日々の活動の詳細（②・git管理外） | その日に何があったかを追うとき |
| `git log` | コミット単位の技術的記録 | コードの変更を追うとき |

**規約: ①（振る舞い・機能）を変えたら、ここに1件書く。** 書式は `### <種別>: <何>` ＋ 意図 ＋ 影響。
種別は `追加` / `変更` / `修正` / `削除`。

---

## 2026-09-05（passcode の置き場を直した。ADR-013 D4）

主人の指示「パスコードの置き場も修正しておきましょう」。

### 変更: passcode を秘密の置き場へ移し、**値そのものを保存しなくした**

`home/config.toml` に平文で入っていた。git には入らない（`home/*` は無視）ので漏れる経路は
無かったが、ADR-009 D4 で「秘密は `~/.manor/secrets/` へ」と決めた自分の線を越えていた。

- 置き場を `~/.manor/secrets/web.json` へ
- **保存するのは塩つきのハッシュ**（PBKDF2-HMAC-SHA256・20万回）。画面は元から現在の値を
  表示しない作りで、**読み出す必要がどこにも無い**——ならば復元できない形で持つ
- **平文を返す関数（`get_passcode`）は消した。** 在る限り、いつか誰かが呼ぶ

### 移行は「足す → 確かめる → 消す」

確かめる前に消すと、書き込みが失敗していたときに**主人が自分のアプリへ入れなくなる**。
起動時に冪等に走り、どこで落ちても平文は残る。照合も「ハッシュ → 平文」の順に見るので、
移行が半分でも入れる。**書き込みが黙って失敗する場合と例外を投げる場合の両方を試験で固定した。**

置き場は home ごとに分けない——分ければ home が2つあるとき混ざらない代わりに、manor の
フォルダを動かした瞬間に passcode を見失う。締め出しのほうが実害が大きい。

## 2026-09-05（第5期の仕上げ: 5g・5h-2・5i）

### 追加: 画面から届かなかった3つを実装（5g。ADR-013）

書き込みコマンド**59件**を機械で洗い、画面の口と突き合わせた。台所・家事・家計・秘書・
タスク・裁定・ルール・タスクの種類は届いていた。届かない3つを実装した。

- **プロジェクトの作成・変更**。ただし**コード（`P1` 等）は作成時のみ**——あとから変えると
  他から参照している記号が全部外れる。編集画面には入力欄を出さず、理由もその場に書く
- **`require_passcode` を画面から**。Tailscale の導入手順から「`config.toml` に手で追記」を
  消した——**方針への正面からの違反**だった
  - **締め出しを防ぐ歯止めを2つ、画面と API の両方に**置いた: パスコード未設定のまま on に
    できない／外に晒して待ち受けている最中は off にできない。画面だけの防御は API から
    抜けられるので、両方でやる
- **メモ（note）の追加**を「AIの進行中 → 伝達」から

### 追加: CLI の多言語対応（5h-2）と英語 README（5i）

- 訳語 689。全グループの `--help` と、結果の行・エラー文
- 差し込み漏れは構造的に例外になる（黙って `{name}` を残す経路が無い）。全キーを実際に
  呼ぶ試験も置いた——この過程で、差し込み名 `{key}` が関数自身の引数名と衝突する設計バグを
  1件発見し、引数を位置専用にして恒久的に塞いだ
- `README.en.md` を新規作成。日本語版は冒頭の相互リンク1行に整理

### 修正: 選択肢に絵文字が戻っていた

主人が ADR-010（2026-09-04）で外させた 🐢🚶🏃 が、**多言語化の過程で戻っていた**
（実機のプロジェクト作成フォームに出た）。外したうえで、**覚えておく話にせず機構で守る**
——主人が選ぶラベルに絵文字が入ったら試験が落ちるようにした。

## 2026-09-05（カレンダー 5d ／ 画面の多言語 5h-1。ADR-012）

主人の指示「5d, 5h を進めてください」。

### 追加: カレンダー拡張（ICS の読み取り）

**予定は秘書の予定表（`secretary_event`）に入れる。新しい表を作らない**——出どころが違う
だけの同じ概念で、別の表にすると「次に来るもの」の答えが2つできる。

- ICS の URL は**秘密の置き場**へ。非公開 URL はそれ自体が鍵で、`config.toml` は④であって②ではない
- **新しいライブラリを入れずに自前で解く**。折り返し・エスケープ・3種類の時刻（TZID／UTC／終日）
- **繰り返しは90日ぶんを展開。単発の予定に上限はかけない**——かけると4か月先の旅行が消える。
  際限なく増える原因は「終わりの無い繰り返し」だけなので、そこにだけ上限を置けば足りる
- **対応できない繰り返しを黙って落とさない。** 最初の1回を入れ、元の指定をメモに残す
- 同期は冪等で、**手で入れた予定には触らない**
- `manor calendar sync` / `list`。秘書のページとダッシュボードの「次に来るもの」に出る

### 変更: `tzdata` を依存に足した（ADR-012 D4 の判断を変えた）

実機で `Asia/Tokyo` どころか **`UTC` すら解決できなかった**——Windows は OS 自体が IANA の
時刻帯データベースを持たない。「ICS を解くための外部ライブラリを入れない」という意図は、
**標準ライブラリが仕様どおり動くための必須データ**まで拒む趣旨ではない。
解決できない時刻帯に当たった場合はローカル時刻に倒すが、**そのことをメモに残す**（黙ってズレない）。

### 追加: 画面の多言語対応（日本語／英語）

**本体は訳文ではなく、抜けない仕組み。** 訳語は 641。

- 日本語が原文、英語は訳。**キーで引く**（文そのものを id にしない）
- **抜けを3重に塞ぐ**: ①`en` の型が `ja` のキー集合を要求する（`tsc` で落ちる）
  ②ja/en のキー集合一致を検算する試験 ③**日本語のべた書きが残っていないか**の機械検算
- 言語は `[manor] language`（`auto`／`ja`／`en`）。**選んだ瞬間に切り替わる**（保存を待たない）
- 担当の名前は訳す（執事 → Butler）。主人が入れたデータは訳さない
- **日付は ISO のまま**とした（英語圏でも誤解なく読め、全面適用は見合わない。ADR-012 D12 追補）

### 修正: 差し込みのある文言が `undefined` になっていた

多言語化が**既存の日本語画面に持ち込んだ後退**。初回セットアップの2枚目で
「undefined に任せたいことを選びます」と出ていた。辞書は正しく、検算試験は緑のまま
——**穴は辞書ではなく呼び出し側**にあった。

`t()` の型を変え、**差し込みが要る文言を引数なしで呼んだら型検査で落ちる**ようにした。
渡した引数の**名前**まではまだ見ていない（正直に限界として記録）。

## 2026-09-05（起動直後だけ画面が出ない。原因は2つ）

> 起動時にブラウザ開いたとき、最初だけ表示されないです。Ctrl+F5 でリロードしたら画面が表示されます。

**独立した原因が2つあり、どちらも「初回だけ・再読み込みで直る」という同じ見え方をしていた。**

### 修正: ブラウザがサーバより先に開いていた

ランチャーは**2秒固定**で待ってからブラウザを開いていた。実測するとサーバが待ち受けるまで
**3.1秒**——ビルドの時間も足すと差はさらに開く。ブラウザは存在しないサーバに繋ぎに行っていた。

固定秒をやめ、**実際に待ち受けるまで待つ**（Windows は netstat、他は `/api/v1/health`。
最大60秒で諦めて開く）。隠れた窓の中でも `timeout` が本当に待つことは実測で確かめた。

### 修正: service worker が古い殻を返していた

画面（HTML）までキャッシュ優先で返していた。ランチャーは起動のたびにビルドし直し、その都度
JS のファイル名（中身の指紋）が変わる。**古い index.html が指す JS はもう存在しない**ので、
画面は真っ白になり、Ctrl+F5（キャッシュを無視する再読み込み）でしか直らない。

画面だけネットワーク優先にした。指紋つきの資産は名前が変われば別物なので、これまでどおり
キャッシュ優先——オフラインでも殻が開く、という当初の意図はそのまま。

### 修正: 無いファイルに index.html を返していた

上の症状が**真っ白**になったのはこれが理由。`<script src=...>` が 404 ではなく 200 の HTML を
受け取り、ブラウザは型が違うとだけ判断して黙って止まる。**原因がどこにも出ない。**
ファイルを狙った経路（最後の区切りに `.` がある）は、無ければ 404 を返す。

## 2026-09-05（名前と絵。「AI Manor」＋主人の icon）

主人の指定: 「favicon を置きました。これを使ってください。名前は "AI Manor" とします」。

### 変更: 表示名を「AI Manor」にした

（同日、いったん `AI Monor` と綴ったが主人の誤字と判明し訂正。**定数1つの書き換えで
全体が直った**——散らばった文字列を都度置き換える形にしていたら、GitHub に上がる
README や webmanifest のどれかを取りこぼしていた。）

**表示名と内部の識別子を分けた。** 画面・文章に出る名前だけが「AI Manor」で、
コマンド（`manor …`）・パッケージ・DB・設定の場所・localStorage の鍵はすべて `manor` のまま
——これらは主人の環境に既に書き込まれていて、変えると動いているものが壊れる。

表示名は**1か所で決める**（`src/manor/branding.py` と `web/src/app/brand.ts`）。散らばった
文字列を都度書き換える形にしない——次に名前が変わったときに書き漏らす。

### 追加: 主人が用意した icon

- タブ・ブックマーク・タスクバーの絵。`.ico` は 16〜256 の7層を持つのでどこでもぼやけない
- ホーム画面に入れたとき用に 192/512 の PNG も置いた（`.ico` は webmanifest では扱いが揃わない）
- **小窓にも同じ絵**。ただし題名は担当の名前のまま（ピン留めが題名で窓を探すため）
- **デスクトップのショートカットにも**。絵はランチャーと同じ場所へ複製してから指す——
  リポジトリの中を直に指すと、フォルダを動かした瞬間に机の上の絵が壊れる
- ショートカットの名前も「AI Manor を開く」に。**古い名前のものは作成時に片付ける**
  （片付けるのは挙げた名前だけ。主人が自分で置いたものには触らない）

### 修正: `/favicon.ico` が 204 を返していて絵が永久に届かなかった

この経路は SPA の受け皿より前に登録されているので、204 を返している限り
`web/dist/favicon.ico` には辿り着けない。**絵が出ない理由がどこにも出ない**形だった。
ビルド前や icon を置いていないときだけ、これまでどおり 204（404 のログを出さないため）。

## 2026-09-05（担当ごとの声。実機で出た4件の修正。ADR-011 §6）

主人が実機を触って出した4件と、「アバターごとに声を設定したい」への応答。**どれも動かして初めて見えた種類の不具合**で、試験は通っていた。

### 追加: 担当ごとの声（ADR-011 D9-D11）

姿は担当ごとに持てるのに**声は1つしか無かった**（「家事担当を女性アバターで作りましたが、声が執事と同じなのが気になりました」）。姿と声は同じ「その担当らしさ」なので置き場を揃えた。

- `[voice.speakers].<担当>` → `[voice] speaker` の順で解決する。指定の無い担当は既定のまま
- 画面は**拡張機能のカードがそのまま使える**——新しい画面は作らない。VOICEVOX のフォームに「既定の話者」＋担当ごとの欄が並び、どれも話者→スタイルの2段で選べる。空欄は「既定を使う」
- **wav の置き場の鍵にはもともと話者IDが入っている**ので、担当ごとに声が違っても混ざらない。作り置き（`manor voice warm`）は重複する話者を1回にまとめる
- 通話は**返事も最初の挨拶も窓の担当の声**。姿が家政婦なのに最初のひとことだけ執事の声、では揃わない。**人格は切り替えない**（中身は執事のまま。ADR-008 §8 D21）

### 修正: 小窓のヘッダ・ピン留め・Light/Dark・ショートカットの黒い窓

- **画面のボタンから開いた小窓にヘッダが残っていた**。API は直っていたが、画面側が古い開き方のままだった。サーバ側でアプリモードに開き、Chrome が無いときだけ従来の開き方に落として**その旨を伝える**
- **ピン留めが押しても何も起きなかった**。窓を題名で探す照合が厳しすぎた（Chrome は題名に別の語を足す）。緩めたうえで、**見つからないときは理由を返す**——黙って何も起きないのが一番困る
- **Light/Dark が設定画面へ行くまで反映されなかった**。起動時に一度も適用していなかったため。最初の描画の前に当てる
- **ショートカットが黒い窓を出していた**。原因は2つ——サーバ起動の行が新しいコンソールを開いていたこと、そして**仲介スクリプトを UTF-8 で書くと Windows が既定の文字コードで読み、エラーも出さず黙って何もしない**こと（UTF-16LE で書く）

### 修正: 設定ファイルに空の行が残る／答えのファイルの BOM

- 担当ごとの声を**初めて**保存したとき、選ばなかった担当が `chef = ""` として書かれていた。入れ子の設定を重ねる処理が、片側がまだ無いときに削除の印ごと写していた。上書きを全部外したら節ごと消える
- `manor setup --answers` が **BOM 付きの JSON で落ちていた**。Windows の PowerShell が既定で書く形なので、主人が用意した答えの中身は正しいのに使えない

## 2026-09-04（総括ダッシュボード・担当の一覧・小窓の作り直し・起動ショートカット。ADR-011）

主人が実機を触って出した7点への応答。

- **総括ダッシュボードを新設**し、サイドバーの先頭に置いた。状態の一行・数字の並び・次に来るもの・24時間の実行・要確認・よく動いているもの。**新しい集計は作らない**——既にある問い合わせを並べ替えて見せるだけ（同じ問いに二つの答えを作らないため）。
- **担当の一覧を新設**（サイドバー2番目）。名前・役職・預かるものの一行・担当ページへ・小窓を開く。
  - 姿は**担当ごとの VRM を1体ずつ1フレームだけ焼いて静止画にする**（描いたら GPU を解放し、結果は覚えておく）。7人ぶんの 3D を同時に生かさない。姿を置いていない担当は輪郭のまま。three.js は**すでに小窓のために配っている実体**を実行時に読むので、依存もビルドサイズも増えない（`web/index.html` に import map を1つ置くだけ）。
- **サイドバーの並びを指定どおりに**（ダッシュボード→担当→タスク→…→拡張機能）。**設定はサイドバーから外して右上の歯車**へ。
- **小窓のヘッダを消した**。`window.open` のポップアップは題名の帯を必ず持つので、`POST /api/v1/face/open` がサーバ側から Chrome のアプリモードで開く（v1 と同じ方式）。
- **リップシンクを取り戻した**（v1 `face_speech.py` の移植）。VOICEVOX の解析結果は合成の**前**に「いつ・どの口の形か」を全部返すので、再生前に予定表を作れる。動かす処理は**別ファイル**（`face-mouth.js`）——ここが壊れても 3D の描画は無事。
- **ピン留めを取り戻した**（v1 `face_pin.py` の移植）。ブラウザからは窓の重なり順を変えられないので、ページの操作をサーバが受けて Windows の API で固定する。Windows 以外ではボタンを出さない。
- **`manor shortcut create`**（ROADMAP 5f。主人が「早めに」と指定）。デスクトップの「manor を開く」をダブルクリックすると、**動いているサーバを止めてから**ビルドし直して起動しブラウザを開く。更新が反映されないと意味がないので、止めてから起こす順序にした。初回セットアップの最後にも置く（既定は作る）。

**訂正**: ADR-008 の棚卸しに「小窓の口を動かす（v1 でも未実装）」と書いたのは誤りだった。v1 の README の古い一文だけを見て判断したもので、実際にはコードが存在していた（`butler/GROWTH.md` G8）。

**あわせて**: 夜勤の打ち切り試験が3度目の偽陽性を出したので、**判定**（決定的）と**機構**（本物の子プロセス0.5秒）に分けた（G9）。

## 2026-09-04（通話＝小窓からの依頼口。ADR-008 §8）

主人の指摘「通話機能が消えている」への応答。v1 `face_talk.py` の移植だが、**主人の訂正で設計を変えた**——雑談の口ではなく、**日常の依頼口**（タスク・予定の追加、進捗の更新、在庫確認）。

- **窓の中は執事そのもの。** manor のワークスペースで `claude -p` を起こすので、`CLAUDE.md`・行動クラス・hooks がそのまま効く。**窓のために新しい門を作らない**（門が二重になると片方だけ直る事故が起きる）。危ないことは既存の HG 固定クラスが止める。
- 道具は `manor …` のコマンド・読み取り系・MCP（カレンダー等）を通す。**任意のシェルと書き換え系は通さない**——主人が「Claude アプリを開くほどでもない件」と引いた線に、道具の面も合わせる。この窓に意味の無いもの（下請けの起動・常駐や予約・別セッションへの発信）は塞ぐ。
- 歯止め: 1日20往復・01:00〜07:30 は押せない・**先に数える**・**残り回数を隠さない**・**失敗は声に出さない**・**会話の中身は残さない**（仕事の結果が DB に残るのは記録であって会話ではない）。
- 小窓が**タブで開いていた**のを 280×340 の窓（画面の右下・担当ごとに使い回し）に直した。
- 実機で確認: ☎ → 挨拶と「本日 2/20（残り18）」→「ゴミ出しのタスクを追加して」→ T2 が DB に入り、残りが 3/20 に。`home/talk/` には `usage.json` だけで会話ログは無い。

**実装中に実測で分かった2つの罠**（どちらも v1 が別の形で警告していたもの）:
- **`--allowed-tools` は制限ではない。** 存在しない道具名を渡しても Bash は普通に実行された。逆に**非対話（`-p`）では事前承認の無い道具は拒否される**——最初の実装は執事が正しいコマンドを組み立てながら実行できなかった。`--permission-mode manual` ＋ 許可のパターン列挙（`tests/behavior/run.py` と同じ形）で解決。
- **組み込みの道具を塞いでも MCP は残る。** 拒否一覧だけを付けた状態で試すと「カレンダー・ドライブ・Notion は繋がっている」と返した。今回は MCP を通す設計だが、**塞ぐつもりなら `--strict-mcp-config` が要る**と実測で確認した。

## 2026-09-04（本番での指摘への応答。ADR-010 §5）

- 「プリセット」を**「自律レベル」**に改名し、主人の画面の選択肢から**絵文字を外した**（慎重／標準／高速）。
- **「タスクの種類が0件」「確認画面で500」の原因は古いサーバのプロセス**だった（更新後の止め忘れ）。`meta.stale` は真を返し警告の帯も実装済みだったが、**帯は枠の中にしかなく、枠を持たない初回セットアップの画面では出なかった**——初めての人が最初に開く画面である。セットアップにも帯を出す。学びは `butler/GROWTH.md` G6。
- プロジェクトの記号で `P1` が弾かれていた。制約自体には理由がある（`P` で始まる参照は project の **id** として先に解決されるため、`P1` という記号は自分の id に隠される）が、**弾かずに黙って小文字へ揃える**ようにした。

## 2026-09-04（初回セットアップの作り直しと「タスクの種類」。ADR-010）

主人が実際に触って出した8点のフィードバックへの応答。**作った側にしか意味が分からないものを、初めての人に見せていた**という一点に集約される。

- **「行動クラス」を主人の画面から消した**（D1）。あれは執事の自律の度合いを決める機構で、タスクの種類ではない。セットアップでは聞かず既定 `general`（L2）で起票する。執事の道具（CLI）には残す。
- **「タスクの種類」を新設**（D2）。人に意味がある札で、既定は8つ（調査・情報収集／検討・設計／作成・実装／修正・改善／資料・文章の作成／連絡・調整／手続き・事務／その他）。**Web から足す・改名・隠す・戻す**ができる（設定の「タスクの種類」節）。「その他」は隠せない。隠しても**過去のタスクに付いた種類は書き換えない**。**種類から自律の度合いは決めない**——札であって門ではない。
- **プロジェクトの期限を聞くのをやめた**（D3）。期日はマイルストーンで表す。
- **語には説明をその場に添えた**（D4）。プロジェクト・タスク・プリセット・種類。フォーカス時ではなく常時表示——触る前に読めないと意味がない。
- **期限は任意だと画面で言うようにした**（D5）。確認画面では空の期限を行ごと出さない。
- **入力欄の重なりを直した**（D6）。行の `min-width` が中の入力欄より狭く、折り返しで隣へはみ出していた。1280/1024/860/600px の4幅で重なり0を実測。
- **全画面の先頭に見出しと一行説明を置いた**（D7）。モジュールを足したときに忘れないよう、登録簿を回して検算する試験つき。
- 隠した種類を**画面から戻す**経路（`POST /api/v1/task-kinds/{id}/unarchive`）を足した——core と CLI にはあったのに Web に無く、画面から隠すと画面では戻せなかった。

## 2026-09-04（Notion 拡張・姿の差し替え・移行の機構化）

- **Notion 拡張（ADR-009 §7。5c）**: `manor notion diary`。中身はその日の DB から機械的に組む（`claude` が壊れていても日記は残る）。**二重投函は2つの機構で防ぐ**——投函前に Notion 側へ日付で問い合わせ、あわせてローカルにも記録する。片方だけでは、手で書いた場合と同時実行のどちらかを取り逃す。API バージョンはコードに固定。禁止語スキャンは Slack の関数を使い回す（二重実装しない）。
- **姿を画面から差し替えられるようにした（ADR-008 §7。5e）**: 「設定」の「姿（小窓）」節。担当ごとに差し替え・削除・小窓を開く。**中身が本当に VRM かを先頭4バイトで確かめ**（拡張子は信用しない）、64MB を上限とし、**一時ファイルへ書いてから置き換える**（失敗しても今ある姿を壊さない）。
- **修正（2度目の事故）**: 新しい中核の表を足したあと `manor init` を忘れた既存の home で、生の traceback が出た（`run` 表に続き `notion_page` 表で再発）。CLI の起動時に**中核の表・列だけ**を冪等に当てる `db.migrate_core` を入れた。**部下のスキーマは当てない**——「導入していない部下」は表の有無で判定しているため。学びは `butler/GROWTH.md` G5。

## 2026-09-04（個人情報の混入を修正・話者を2段選択に）

- **修正（個人情報）**: 主人の名（かな）が ADR-007 の「例」から docs・試験・画面のプレースホルダへ広がり、**git 管理下の8ファイル**に入っていた（主人の指摘）。すべて一般名（`旦那様`／`ご主人様`）へ置換。リーク語彙リストに「値として現れる形」だけを拾う3行を足し（`聞かず` 等の普通の日本語に当たらないよう限定）、pre-commit が実際に止めることを確認した。学びは `butler/GROWTH.md` G4。
- **VOICEVOX の話者を「話者 → スタイル」の2段で選べるようにした**（主人の指摘。127件の平らな一覧では探せない）。`options()` の要素は `group`／`member_label` を任意で持てる（ADR-009 D17）。保存される値は従来どおり1つで、契約は変わらない。実機で43話者・先頭は6スタイルを確認。

## 2026-09-04（拡張機能。ADR-009 5a）

- **外部に依存するものを「拡張機能」として本体から切り離した**（主人の方針。ROADMAP §11）。**拡張が1つも入っていなくても manor は完全に動く**を不変条件にした。
- Web アプリのサイドバー最下部に「拡張機能」。カードに状態（未導入／設定が要る／準備完了／動作確認済み／エラー）が出て、開くと**導入手順・設定フォーム・「試す」**が並ぶ。README を読ませない。
- **VOICEVOX の話者を名前で選べるようにした。** これまでは `home/config.toml` を手で開いて `speaker = 13` という意味の分からない数字を書く必要があった。エンジンから一覧を取り、「四国めたん（ノーマル）」のように選ぶ（実機で127件・保存・疎通まで確認）。エンジンの探索先に macOS と Linux を足した。
- 秘密（API キー等）は `~/.manor/secrets/<id>.json`（**リポジトリの外**）。API は `has_<key>` しか返さず、値を読み出す口を作らない。CLI の `manor ext set --secret` は標準入力から読む（履歴に残さない）。
- Tailscale の検出が PATH しか見ておらず、**入っているのに「未導入」と出た**（Windows の Tailscale は PATH に載らない）。標準の場所も探すよう直した。
- CLI: `manor ext list|show|set|test`。
- **Slack 拡張（最初の実物。ADR-009 5b）**: `manor slack brief|inbox|test`。送信は既定で DB から機械的に組む（`claude` が壊れていても届く）。**送信の直前に禁止語スキャン**——1つでも当たれば何も送らず、当たった語そのものは出力しない。判断待ちは **decision ごとに別メッセージ**で送り、スレッド返信を 1:1 で引く。返信先頭の `D3` のような id は推測ではないので優先して使う。`修正: <一言>` は差し戻しとして受ける。引けない返信は `home/inbox/` へ落とし、**推測で裁定しない**。定期実行は `manor night` に寄せ、別の常駐は作らない。

## 2026-09-04（拡張機能の枠組み。ADR-009 5a）

- **追加**: `src/manor/extensions/`（明示的な登録簿。ADR-009 D2）。各拡張は `MANIFEST` と `detect()`/`check()`（任意で `options()`）を持つ1ファイル。import 時に MANIFEST の形を検算し、壊れていれば起動時に分かる。状態は5つ（`not_installed`/`needs_config`/`ready`/`ok`/`error`。D3）——`detect()` が未導入かどうかを、保存済み設定が `needs_config` かどうかを、`check()`（`test()` 経由・押されたときだけ）が `ok`/`error` を決める。結果は `home/extensions/state.json` に残る。
- **追加**: `voicevox` 拡張（ADR-009 D9）。**`speaker = 13` を手で書く問題を解く**のが本題——話者を `GET /speakers` から取得して名前で選べるようにした（`options("speakers")`）。保存すると既存の `[voice] engine = "voicevox"` へそのまま書く（新しい `[voicevox]` 節は作らない。互換を保つ）。`voice._default_engine_path()` を macOS/Linux へベストエフォートで拡張した。
- **追加**: `tailscale` 拡張。設定項目を持たない——`detect` はコマンドの有無、`check` は `tailscale status`。状態を変えるコマンドは実行しない。`[web] require_passcode` の案内をここに寄せた。
- **追加**: `src/manor/secrets.py`（`~/.manor/secrets/<id>.json`。リポジトリの外・`home/` の外。D4）。**読み出しの口を作らない**——web の API 層は `has_<key>: bool` しか返さない。
- **追加**: `GET/PUT/DELETE /api/v1/extensions[/{id}]`・`POST /api/v1/extensions/{id}/test`・`GET /api/v1/extensions/{id}/options/{name}`（ADR-009 D6）。`manor ext list|show|set|test`（D8。`--secret <key>` は標準入力から読む）。
- **追加**: フロントの「拡張機能」画面（サイドバー最下部。`web/src/modules/extensions/`）。カードで状態を示し、開くと導入手順・設定フォーム（`options_from` が空を返せば数字直接入力に落とす）・「試す」を出す。パスワード欄は値を描画せず「設定済み」の印だけ。

## 2026-09-04（声の機構 `manor voice`。ADR-008 第2期）

- **追加**: `manor voice status|on|off [--by-night]|restore|say|warm|engine {start,stop,status}`（v1 `apps/butler-face/{speak.ps1, engine.ps1, voice.ps1, warm.ps1}` の Python 移植。ADR-008 §6 D7〜D13）。`home/config.toml` の `[voice] engine = "voicevox"` が無ければ「未設定」として振る舞い、`speak()` は何もせず `False` を返す——**VOICEVOX は依存にしない**（`pyproject.toml` は変えない）。
- **追加**: 消音の印はディスク（`home/voice/state.json`）。v1 の事故（2026-09-02。消音の状態をプロセス内に持っていて、夜勤がプロセスごと消えて丸一日黙った）を踏まえ、戻す口を `voice.restore()` の1本に絞った（`muted_by_night` が立っているときだけ戻す）。**不変条件**: 主人が自分で消した消音は夜勤をまたいでも決して戻さない——`mute(by_night=True)` は既に手動で消音されているときは `muted_by_night` を立てない（`muted_by_night` は「夜勤が黙らせた」を意味する旗であって、「夜勤が走っている間ずっと立つ旗」ではない）。
- **追加**: `night/runner.py` が夜勤の開始時に `restore()` → `mute(by_night=True)`、終了時（正常・異常どちらでも）に `restore()` を呼ぶ（D10。声の失敗は夜勤自体を止めない）。
- **追加**: `manor check` に C13（`muted_by_night` が立ったまま夜勤の lock が無い。警告のみ。D12）。
- **変更**: `notify.py` の発話の門を `voice.speak()` に一本化した（D8）。`[voice]` が設定されていればそちらを先に試し、未設定なら従来の `speak_command`／OS 既定へ落ちる。**どちらの経路でも消音を尊重する**——判定は `voice` の中に1箇所だけ。`notify.all_phrases()` を追加し、`manor voice warm` はこれ経由で決まり文句を作り置きする（文面の出どころを1箇所にする。D11）。
- キャッシュは `home/voice/cache/<sha256(text|speaker)>.wav`。HTTP は `urllib`（標準ライブラリ）のみ、新しい依存は足していない。

## 2026-09-04（v1 からの引き継ぎ 第1期。ADR-008）

- **小窓（`/face`）を復旧した。** `manor board` を Web アプリの別名にしたときの取りこぼしで、姿の小窓はどのサーバからも 404 になっていた（旧 `python -m manor.board` にしか無かった）。Web アプリの経路として作り直した。
- **姿を担当ごとに持てるようにした**（`home/face/<agent>.vrm`。`model.vrm` は執事の別名として残す）。`/face?agent=chef`・`manor face --agent chef`。差し替えはファイルを置くだけで、再起動も再ビルドも要らない。姿が無ければ輪郭と置き場の案内に落ちる。
- **姿の実体だけを認証の内側に置いた**（執事の検分で追加）。`/face/model.vrm` が配るのは②の資産で、`tailscale serve` はループバックへ流すため、素通りだと tailnet の誰でも取れてしまう。器（`/face`）は素通りのまま——ここまで止めると cookie の切れた小窓が JSON のエラー塊になって画面の隅に居座る。
- v1 の棚卸しと移行の順番を [ADR-008](docs/design/ADR-008_v1_migration.md) に記録した。**「v1 と競合するので外部連携はやらない」の制約は失効**（主人が v1 を畳む意思を示したため）。

## 2026-09-04（姿の小窓を Web アプリへ戻す。ADR-008 第1期）

- **修正**: `manor board` が Web アプリの別名になって以来 404 だった `/face`（姿の小窓）を、`src/manor/web/` の機能として復旧した（ADR-005 裁定13 の取りこぼし。ADR-008 D2）。独立 HTML のまま（SPA には載せない）。**SPA のキャッチオールより前に登録**しないと飲み込まれるので、`web/app.py` の配線順に注記を足した。
- **追加**: 姿を担当ごとに置けるようにした（ADR-008 D3）。`home/face/<agent>.vrm`（`butler`/`chef`/`housekeeper`/`steward`/`secretary`/`qa`/`auditor`）。`home/face/model.vrm` は `butler.vrm` が無いときだけ読む後方互換。`GET /face?agent=<name>` の題と画面上の表示名は担当の日本語名（`src/manor/agent_meta.py`。部下モジュールの `LABEL` があれば再利用し、無い担当だけ補う）。語彙外・`..`／区切り文字入りの `agent` は 404（`home/face/` の外を絶対に読まない）。
- **追加**: `manor face [--agent <name>] [--port] [--corner] [--width] [--height] [--dry-run]`（v1 `show-face.ps1` 相当。ADR-008 D4）。Chrome が見つかれば `--app=` のアプリモードで開き、隅への配置は `tkinter` で画面サイズが取れたときだけの best-effort（v1 の Win32 `MoveWindow` は持ち込まない——OS 固有スクリプトを持たない設計方針のため）。Chrome が無い・起動できない・位置が取れない、いずれも例外にせず1行の案内で続ける。
- three-vrm 一式（`vendor/`）は `src/manor/board/static/vendor/` から `src/manor/web/face_static/vendor/` へ**コピー**（旧 board 側は壊さない）。
- 試験: `tests/web/test_face.py`・`tests/test_face_cmd.py` を追加（889件 → 追加ぶん含め全通過）。

## 2026-09-03（構成図の描き直し）

- `docs/diagrams/` の3枚を現行の姿（Web アプリ・関門・authorizer・`authorized_by`）で描き直し、**task の状態機械**を1枚足して4枚にした（archify・showcase）。
- README に「構成図」の節を新設し、4枚を画像で載せた。画像はビューアの **Export → PNG**（正規書き出し。操作系 UI を含まない高精細画像）で作り、`docs/diagrams/png/` に置く。対話版 `*.html` へのリンクも併記。
- 凡例を4図とも日本語にした（`meta.legend.entries`）。
- **README を 254 行 → 135 行に絞った**（GitHub のリポジトリとして読める量に）。図は要点の2枚だけ載せ、残り2枚は対話版へのリンクに。部下ごとの長い説明は `docs/staff/`、CLI 早見表は `--help`、画面一覧は `docs/web.md` に委ねた。あわせて古い記述を直した: 「ダッシュボード（素の JS 製）が主人向けの画面／Web アプリの実装はこれから」（ADR-004・005 は実装済み）、`/api/staff/<name>`（現在は `/api/v1/`）、試験件数 560 → 840 超。
- 検分で見つけて直したもの: 裁定図のラベルが左端で切れる（`column_fit: "spread"`）／状態機械の3本目のレーンが空で `Outcomes` と英語のまま（`done`・`withdrawn` を移動）／構成図で `import-v1／--sync（唯一の直接 INSERT 例外）` が隣のラベルに食われる（`labelAt` を置き直し）。**showcase の validate が 0 warnings でも、書き出し画像で文字が欠けることがある。**

## 2026-09-03（初回セットアップ。ADR-007）

- **追加**: 初めて manor を導入する人が、対話ではなく決まった型で「主人の呼び名・執事の呼び名・
  主な用途・最初のプロジェクトとタスク」を入れられるようにしました。真実は core の新しい表
  `profile`（`src/manor/profile.py`）。登録は既存の機構（`project.add` / `task.add`）を1つの
  トランザクションで通すので、途中でクラスが語彙外などの理由で失敗すればプロジェクトも含めて
  全部戻ります（D2）。
- **CLI**: `manor profile show|set` `manor setup --answers <json>|--status`。
- **API**: `GET/POST /api/v1/setup` `PUT /api/v1/setup/profile`。`GET /api/v1/meta` に
  `setup_done` を追加（フロントの `/setup` 誘導用。D6 は別担当）。
- **射影・起動時注入**: `home/projections/PROFILE.md` を追加（他の射影と同じく `manor check`
  C7 の対象）。セッション開始時、プロフィールの要約1行を `manor active` の本文の前に置く
  （`master.callname` 未設定なら何も足さない。D3）。

## 2026-09-03（初回セットアップ。ADR-007）

- **新設**: 初めて導入する人が決まった型で「呼び名・主な用途・最初のプロジェクトとタスク」を順に入れる画面（Web の `setup` モジュール。5段。`meta.setup_done` が偽なら `/setup` へ誘導）。答えは DB の `profile` 表が真実で、プロジェクト・タスクは既存の `project.add` / `task.add`（`--class` が level を決める）を1トランザクションで通す。
- 射影 `home/projections/PROFILE.md` を追加（C7 の対象）。起動時の hook が「主人の呼び名 ／ 執事 ／ 用途」の1行を射影の前に注入する。
- CLI: `manor profile show|set`、`manor setup --answers <json>` / `--status`。API: `GET/POST /api/v1/setup`、`PUT /api/v1/setup/profile`。設定画面に「プロフィール」節と「セットアップをやり直す」。
- 行動クラスに `general`（一般の作業。L2）を追加。ウィザードの既定クラス。執事の起票でも「どのクラスにも当たらない」ときの受け皿（level を自分で決める逃げ道ではない）。
- **同日、主人の指示で聞く量を絞った（ADR-007 §6）**: 用途は「使いたい機能」（タスク・料理／買い物・家計・家事・予定）として聞き、選んだ機能の段だけ出す（最少 3 段、最多 5 段）。全段に「あとで」。既定は推奨値（呼び名を飛ばせば `ご主人様`、執事は `執事`、クラス `general`、通貨 `JPY`）。入力欄は例のプレースホルダー。台所の答えは `chef_taste`（人数・アレルギー・苦手）へ、家計の前提（家計簿アプリ・通貨）は `profile` へ。

## 2026-09-03（再レビューへの応答。ADR-006 D19–D23）

- **修正（記録消失）**: `manor archive` が、アーカイブ済みの月に遡って追記された記録を2回目の実行で捨てていた（保存先が既にあると書かず、元からは消していた）。保存先へ追記し、保存先に書けてから元を一時ファイル経由で書き換える順序に変更。索引行は累計件数。表形式の保存先には見出し行と区切り行を複製する（D19・D20）。
- **検査**: C5 を `task_event.authorized_by`（遷移を通した裁定）で判定するよう書き直し。`decided_by` の辺は「関係」、`authorized_by` は「事実」で、検査は事実を見る。C12（`authorized_by` の指す decision/handoff が実在すること）を新設。`db.init` が辺→事実の一回きりの写しを行う（D21）。
- **関門**: 対応表に `.claude/skills/*/SKILL.md`・`src/manor/{task,policy,cli}.py` を追加。①層のファイルがすべて対応表・接頭辞規則・除外一覧のどれかに当たることを検算する試験を追加（改名で黙って関門が外れる穴を閉じる。D22）。
- **run stats**: 合計に計測できた件数を添える（「$1.20（10件中7件を計測）」。計測 0 件は `—`。D23）。
- **3周目（同日）**: `manor archive` の追記の継ぎ目（保存先が改行で終わっていないと2件が1行に融合する）を塞いだ。done への遷移の `authorized_by` は decision id に限定（`H<n>` と実在しない id は `task.status` が拒み、C12 も done では decision のみを正当とする。C5 と C12 が同じ答えを出す）。空文字の `authorized_by` は「未指定」と同じ扱い。辺は門の入力・`authorized_by` は検査の入力と ADR-006 D21 に書き分け。

## 2026-09-02

### 追加: manor v2 を新設

**何を**: AI執事 v1 の改良版として `manor` を新設しました。SQLite を唯一の書き手にし、
タスク間の関係をグラフ（`node` / `edge`）として持ち、規則を散文ではなく hooks・CLI の状態機械・
試験で守る構成に変えています（設計は `docs/design/ADR-001_core.md`）。

**なぜ**: v1 は Markdown が真実であるために手で書く不整合が生じ、タスク間の関係を持てず、
散文の規則は執事が覚えている前提でしか守られませんでした（`ROADMAP.md` §1）。

**どう影響するか**:

- 憲法（`CLAUDE.md` `butler/*.md`）は v1 から薄く移植し、経緯を含む物語は書かないことにしました
- `home/` 配下（DB・主人の情報・環境固有）は git 管理外です
- v1 は読み取り専用の参照元として残ります。移行や実データの取り込みは別段（`ROADMAP.md` 段3）

### 追加: core — SQLite が唯一の書き手になった（ADR-001）

**何を**: `manor` CLI（task / project / decision / milestone / note / handoff / ctx / render / check / active）と、
`node` / `edge` によるグラフ、状態機械、`butler/policy.toml` の解決器、4 つの hooks を実装しました。

**なぜ**: v1 の不整合 15 件中 8 件は「手で状態を書く」ことに起因していました。書く場所を 1 つにすれば、
その 8 類型は構造的に起きません（`tests/test_v1_classes.py` が 1 類型 1 本で検算）。

**どう影響するか**:

- `home/projections/*.md` と `home/STATE.md` は生成物です。**Edit/Write は `PreToolUse` hook が拒否します**
- HG のタスクは、結ばれた decision が承認されていなければ `done` にできません
- `waiting` は理由が必須、`done`/`withdrawn` からは戻れません。戻すなら新しいタスクを起こして `supersedes` を張ります
- 委譲は `manor handoff new` が指示書（10 節）を生成し、報告は 5 見出しが揃わないと受け付けません。
  却下すると owner は執事へ戻り `hold` になります

### 追加: 部下 4 名 — chef / housekeeper / steward / secretary（ADR-002）

**何を**: 料理長・家政婦・家令・秘書を core のプラグインとして追加しました。定義は `.claude/agents/`、
道具は `manor chef|house|money|sec ...`、データは各自の `<name>_*` 表です。

**なぜ**: 「繰り返し出てくる、完結した推論を持つ領域」だけを担当にする、という基準で 4 つに切りました。
健康は chef の週次に、司書は secretary の inbox 仕分けに含めます。

**どう影響するか**: 道具は判断しません（並べ替え・突き合わせ・集計・期日計算まで）。**家令は支払いをしません**。
部下同士の書き込みは家政婦 → 料理長の買い物リストだけです。相対日付は秘書の `resolve-date` が唯一の出どころです。

### 追加: v1 からの取り込みとグラフの問い合わせ（ADR-003）

**何を**: `manor import-v1` が v1 の `QUEUE.md` / `PROJECTS.md` を読み（v1 のパーサをコピー。読む側だけ）、
task / decision / project / milestone と辺（`depends_on` は状態欄から、`content` の言及は `relates_to`、
裁定済みの決定への参照は `decided_by`）を作ります。`manor graph dups|blocked|stale|stats` を足しました。

**どう影響するか**: 冪等です（2 回目は増えません）。`v_blocked_ready` は決定も見るようになり、
「裁定は済んだのに待っている」を C1 が拾います。

### 追加: 境界を試験で守る

**何を**: `tests/test_privacy_boundary.py` が本物の git で `check-ignore` を回し、②④が落ち ①③が残ることと、
ユーザー名入りの Windows パスが追跡候補に無いことを検算します。

**なぜ**: 「集約した」と書くだけでは守れません。初回の実行で `ROADMAP.md` に混ざっていた
ユーザー名入りのパスを実際に捕まえました。

### 修正: 検分（QA）の指摘2件 — 射影保護の判定と pre-commit の配線

**何を**: ①`PreToolUse` hook が `home` という**フォルダ名**でしか射影を見分けておらず、`MANOR_HOME` を
別名のフォルダへ向けると本物の射影への Edit が素通りしていました。`MANOR_HOME` を解決して実体で比べる段を
足しました。②`.githooks/pre-commit` は置いてあるだけで、git は既定で見に行きません。`manor init` が
`core.hooksPath` を設定するようにしました（`.git` と git があるときだけ。冪等）。

**なぜ**: どちらも「命じてある」と「塞いである」の差でした（ADR-001 D6）。検分役が実際に破りに行って見つけています。

**どう影響するか**: 既定の配置（`<repo>/home`）では挙動は変わりません。`git init` の後にもう一度 `manor init` を
回すと pre-commit が効きます。

### 追加: 第2期 — ダッシュボード・声かけ・担当との直接対話・齟齬検査（2026-09-02）

**何を**:

- **`manor board`**（`src/manor/board/`）: v1 butler-board 相当の5画面（要対応／AIの進行中／計画／記録／**家**）を
  DB の上に。裁定・状態変更・委譲の受け入れは画面から manor の API 経由で書き、射影も更新します。姿の小窓 `/face`
  は `home/face/model.vrm` があれば VRM を出します。`--host` は既定 `127.0.0.1`（Tailscale 用に変えられます）
- **`manor notify`**: 要対応が増えたときだけ一度鳴らす（v1 と同じ文面・静穏時間・初回は黙る）。Stop hook から自動。
  声の実体は `home/config.toml` の `speak_command`（既定は OS 標準の音声）
- **`manor talk <name>`**: 担当と直接話す（`claude --agent <name>`）。`CLAUDE.md` に「担当として起動されたとき」の節
- **`manor import-v1 --reconcile`**: v1 の Markdown を再パースして DB と1件ずつ突き合わせる（本番データで 1,873 項目一致・齟齬 0）
- **`tests/behavior/`**: 自然言語 → DB の振る舞い試験（`claude -p` をサンドボックス home で回し、DB の副作用で判定）

**なぜ**: 主人が「自分で使ってチェックできる状態」にするため（第2期の要望）。外部連携（Slack・Notion・カレンダー）は
v1 と競合するので作っていません。

### 修正: 振る舞い試験で落ちた2本を機構で直した（2026-09-02）

**何を**: ①`depends_on` を張ると、相手が未完了なら src を機械が `waiting` にする（`--no-wait` で抑止）。
②HG のタスクは `--recommendation` が必須で、起票と同時に decision を積み section A へ入る。③`--class` を
渡したら level はクラスから決まり（明示の `--level` は無視）、存在しないクラスは拒否、`task add --help` に
クラス一覧を出す。④`CLAUDE.md` 自律の原則に「情報が足りなくても先に起票する。聞くのは起票してから」。

**なぜ**: 自然言語 → DB の試験で、執事が「辺は張ったが待ちにしない」「HG を起票したが decision を積まず
聞いて終わる」「level を自分で決める」の3つを実際にやった。①〜③は機構で消え、④だけは散文でしか直せない
（「起票する前に聞くな」は機械が強制できない）。追試で S4 2/2・S6 3/3 PASS。

### 修正: ダッシュボードを v1 と同等に／既定ポート 8788／プロジェクト状態の取り込み規則（2026-09-02）

**何を**: ①主人の指摘（ステータス別／プロジェクト別の切り替え不能、計画→プロジェクト表の崩れ）を直し、
v1 README §2 と app.js を 43 項目で突き合わせて欠落を全部埋めた（`docs/board_parity.md`）。原因は
`index.html` の `id="panel-running"` 欠落（JS のハンドラが付かない）と、CSS の `nowrap` が自由文の列に
効いていたこと。②`manor board` の既定ポートを **8788** に（v1 の 8787 と共存。v1 の show-face は 8787 の
board を入れ替えるため）。③v1 の PROJECTS.md の状態欄が「完了」で**始まる**ときだけ `done` に
（「9/11軸完了」のような途中経過を done にしていた。実データで 5 件を戻した）。

**どう影響するか**: ダッシュボードは http://127.0.0.1:8788/ 。v1 と同時に立ち上げてよい。

### 追加: 夜勤の仕組み `manor night`／修正: 取り込みの優先度の向き（2026-09-02）

**何を**: ①v1 の夜勤（指示書 `home/night/tasks.md` → `claude -p` → 作業報告 `home/night/reports/<日付>.md`）を
Python で移植。起動の門（締切まで足りなければ起動しない）・時刻の注入（執事に時計を読ませない）・
締切＋猶予での打ち切り・ロック・利用上限からの1度だけの再開・`MANOR_HOOKS=off`・道具の絞り込み
（外部送信の道具は無い。git は `add`/`commit` まで）は runner が守ります。**タスクスケジューラへの登録はしていません**
（`manor night install --dry-run` が登録コマンドを見せるだけ。v1 が現役の間はトリガーしない）。
②v1 の優先度（★の数。3 が最高）を manor（1 が最高）に写すとき反転していなかったのを直し、本番 DB の 16 件を揃えました。

### 変更: 裁定の一言は省略可（修正だけ必須）／修正: 入力中のフォーカス・ツリーの `[pj]` 表記（2026-09-02）

**何を**: ①承認・却下は一言なしで押せる（既定の一言「承認」「却下」が入る）。修正だけは指示文が必須。
CLI の `manor decision rule --ruling` も省略可に。②5秒ごとの再描画で入力欄が作り直されフォーカスが外れていた。
入力中（IME 変換中を含む）はその画面の再描画を飛ばす。**HTML に `id="panel-judge"` が無くガードが効いていなかった**ので
id を足し、JS が参照する id が HTML に実在することを試験で検算するようにした（同じ型の不一致が2度目）。
③プロジェクト別ツリーの行から `[p3 …]` の接頭辞を落とした（親行がプロジェクトなので冗長。ステータス別では残す）。

**なぜ**: 主人の実機確認（2026-09-02）での指摘3件。

### 追加: `manor init --demo`（合成データの家）／README 全面改稿（2026-09-02）

**何を**: 空の home にだけ、架空の家庭の合成データ（プロジェクト3・タスク12・要対応1・部下4名のデータ）を入れる
`--demo` を足しました。README を「これは何か／できること（機能一覧）／インストール／使い方（CLI 早見表）／
仕組み／v1 から移る／公開・共有／これから」の構成に書き直しました（画面の撮影は載せていません＝本番データが写るため）。

**なぜ**: 主人の要望⑤「GitHub に上げて誰でも使える形式」。試せる家が無いと README は読まれません。

### 追加: 家庭用 Web アプリ（第3期・2026-09-03）

**何を**: ①バックエンド `src/manor/web/`（API v1 `/api/v1/<module>/`・ループバック以外では passcode 認証・
`manor web serve|build|install`。既定ポート 8789）②フロントエンド `web/`（Vite + TypeScript + React。
画面＝モジュール: tasks / kitchen / house / money / secretary / rules / imports / night / settings。PWA）
③core に**家庭のルール**（`rule` 表・`manor rule`）と**家計簿 CSV の取り込み**（`manor money import`。同じ行は二重に入れない）。

**なぜ**: 主人の方針（ADR-004）「執事ダッシュボードではなく管理アプリ。タスク管理はその機能の一つ」。

**どう影響するか**: 素 JS の `manor board`（8788）はそのまま動きます。同等性表を Web アプリが満たしたら別名にします（ADR-005 §5）。

### 変更: `manor board` は Web アプリの別名に／起動時の移行／git 管理開始（2026-09-03）

**何を**: ①結合で見つかった 12 件のずれ（型・列挙・書き込み口の欠落・入力中の巻き戻り・滞留バッジ）を直し、
同等性表 41○/2△/0×。`manor board` は Web アプリを 8788 で立てる別名にしました（素 JS の旧画面は
`python -m manor.board` でのみ）。②`create_app` が起動時に冪等な移行（`db.init`）を当てます——本番 home で
`manor init` を忘れると `/api/v1/rules` が 500 になったため。③`git init`・`core.hooksPath`・最初のコミット（255 ファイル。
`home/` は README だけ）。CI は `.github/workflows/test.yml`。

**なぜ**: 主人「完成まで進めて OK」。

### 追加: LICENSE（MIT）／`[web] require_passcode`／Tailscale の手引き（2026-09-03）

**何を**: ①主人の選択で MIT。②`home/config.toml` の `[web] require_passcode = true` で、ループバック待ち受けでも
passcode を要るようにした。`tailscale serve` は HTTPS を終端して 127.0.0.1 へ転送するため、ホストだけ見ると
「ループバック＝認証なし」になっていた（主人の導入時に判明）。③`docs/tailscale.md`（A: tailnet の IP に直接／
B: `tailscale serve` で HTTPS＋PWA。執事の推奨は B ＋ require_passcode）。

### 追加: 外部レビュー（2026-09-03）への応答 — 関門・証跡・計測・寿命・隔離（ADR-006）

**何を**: ①`manor gate`: ①層（CLAUDE.md・butler/・agents・policy.toml・hooks）が変わったコミットだけ、関係する
振る舞いシナリオを pre-commit で回す（`MANOR_GATE=off` で意図的に飛ばせる）。②承認の証跡: `decision.evidence`
（何を見て推奨したか）と `task_event.authorized_by`（どの裁定に基づいて動いたか。逆引きは `decision show`）。
③`run` 表と `manor run list/stats`: 夜勤と振る舞い試験の `claude -p` がモデル・トークン・費用・所要・終了理由を
1 行ずつ残す。「この委譲は直列より安いか」は数字で決める（AGENTS.md）。④`manor archive`: 追記ファイル
（CHANGELOG・GROWTH・LOG）を月ごとに畳む。40KB 超は `manor check` の C10 が警告。⑤部下の表の隔離を
SQLite の authorizer で機構化（自分の接頭辞以外への書き込みは拒否。例外は家政婦→料理長の買い物リストだけ）＋静的検算。
Web: 要対応カードに根拠、設定に「稼働と費用」。

**残る限界（明記）**: Python から直接接続すれば authorizer は掛からない（担当の定義が守る）。執事のセッション内の
委譲（Agent ツール）の費用は CLI から測れない。
