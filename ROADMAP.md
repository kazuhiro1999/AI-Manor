# ROADMAP — manor（AI執事 v2 ＋ 家庭用エージェント群）

作成: 2026-09-02 ／ 設計責任: 執事（Claude Fable）／ 実装・文書化: Sonnet への委譲
参照元（読み取り専用）: v1 ワークスペース `AI執事/`（このリポジトリの外・ローカル）／ 外部レビュー `AI執事レビュー_2026-08/02_レビューと改善提案.md`（同）

> **このファイルの役割は「文脈とゴールを忘れないこと」。** セッションが切れても、
> ここを読めば「何のために・どこまで・次に何を」が復元できる状態を保つ。
> 進捗は末尾の「現在地」を更新する。**判断の記録は `docs/design/ADR-*.md`。**

## 0. ゴール（主人の言葉から）

| # | ゴール | 完了の定義 |
|---|--------|-----------|
| **G1** | 現 AI執事の**改良版**を作る | `manor/` を Claude Code で開けば執事として起動し、タスク・プロジェクト・判断が **SQLite を唯一の書き手**として回る。**改善点を1つ以上、根拠付きでレポート**（`docs/reports/01_執事改良レポート.md`） |
| **G2** | 執事とは別の**部下エージェント**（家庭用）を作る | 各担当が定義（`.claude/agents/*.md`）・データ（DB）・道具（CLI）・試験を持ち、**機能と役割をレポート**（`docs/reports/02_家庭用エージェント群レポート.md`） |

### 制約（変えない）

- **GitHub 公開前提**。個人情報・案件・環境固有は **`home/` に閉じ込め、git から外す**（v1 の ①②③④ の切り分けを継承。境界は `.gitignore` ＋ 試験で機械が守る）
- **v1 フォルダは読み取り専用**（コピーは可）。v1 を壊さない
- **設計判断は執事（この文書の書き手）が行い、コーディング・文書作成は Sonnet に委譲**する

## 1. v1 の何を直すのか（レビューと意見箱から、根拠つき）

| # | v1 の痛み | 根拠（v1 内の記録） | v2 での解 |
|---|-----------|---------------------|-----------|
| A | **Markdown が真実で、手で書くから不整合が出る** | `03_design/タスクデータをDBにする.md` §1: 1日で見つかった不整合15件のうち **8件は DB なら構造的に消える**。DB 化は段2（突き合わせ）で止まっている（意見箱 I8） | **SQLite を唯一の書き手**にする。Markdown は生成物（射影）。**射影への Edit/Write を hook が拒否**する |
| B | **タスク間の関係を持てない**（重複・順序・同一が残る） | 意見箱 I10「同じものを指すタスクが二重にできる／残り続ける」／ I6「進行中が進捗を表さない」 | **Context Graph**: ノード（task/project/decision/agent/note）と型付きエッジ（depends_on / duplicates / supersedes / part_of / decided_by / delegated_to …）を同じ SQLite に持ち、`manor ctx <id>` で**必要な文脈だけ**を組み立てて渡す（just-in-time retrieval） |
| C | **散文の規則は破られる** | GROWTH G43/G44/G46（起票スキップ・委譲中の放置・声かけ忘れ）は全て「執事が覚えている」ことに依存。レビュー提案4「hooks は未使用の最大資源」 | 規則を **hooks・CLI の状態機械・policy.toml・試験**に移す。「命じてある」と「塞いである」を区別し、**塞いだものだけ憲法から消す** |
| D | **常時ロード層の肥大**（178KB≒5〜6万トークン） | レビュー §0・提案1・2。CLAUDE.md 179行 12.4KB | CLAUDE.md を **60行以内**に。起動時は `manor active`（射影）だけを hook が注入。経緯は CHANGELOG/GROWTH へ |
| E | **委譲のプロトコルが無い**（自然文で返り、執事が手で写す） | 意見箱 I8「4通とも自然文で返ってきて、執事が読んで手で書き写した」 | **handoff を DB のレコード**にする。`manor handoff new` が役割・スコープ・Project Policy・制約・検証要件を DB から**生成**し、報告も構造化して受け取る |
| F | **移植性**（Windows 依存の .ps1 が道具の大半） | `03_design/移植性の設計.md` 規則2「新しく作る道具は Python（uv）」 | **道具は Python のみ**（`src/manor/`）。hooks も `python -m manor.hooks`。`.ps1` を core に置かない |

## 2. 組織（誰が何を預かるか）

```
主人
 └─ 執事 butler（統括。判断・裁定・報告。実務はしない）        … 本セッション／CLAUDE.md
     ├─ 料理長 chef        … 台所: 在庫・食事の記録・買い物・好み
     ├─ 家政婦 housekeeper … 家: 家事の当番・消耗品・設備の手入れ・ゴミの日
     ├─ 家令   steward     … 家計: 支出の記録・定期支払い・予算（**支払いはしない**）
     ├─ 秘書   secretary   … 予定・控え（リマインド）・受け渡し置き場（inbox）の仕分け
     ├─ 検分   qa          … 作ったものを検める（直さない）        … v1 から継承
     └─ 監査   auditor     … 憲法を月1で外から検める               … v1 から継承
```

**担当を作る単位は「繰り返し出てくる、完結した推論を持つ領域」だけ**（レビュー提案6）。
「なんでも屋」を作らない。1人1領域。各担当は **自分の表（`<name>_*`）にしか書かない**。

## 3. 段取り

| 段 | 何を | 誰が | 状態 |
|:--:|------|------|------|
| **0** | ロードマップ（本書）・設計判断（ADR-001）・スキーマと CLI 契約 | 執事 | ✅ 2026-09-02 |
| **1a** | **core**: `src/manor/`（db・graph・task・project・decision の CLI と試験） | Sonnet（実装役） | ✅ 2026-09-02（試験98件・3,075行。2a の機構も同梱） |
| **1b** | **憲法と骨格**: `CLAUDE.md`・`butler/{SOUL,POLICY,AGENTS,INTENT}.md`・`policy.toml`・`.gitignore`・`pre-commit`・`README.md` | Sonnet（文書役） | ✅ 2026-09-02（CLAUDE.md 50行・3.0KB） |
| **2a** | **機構**: hooks（SessionStart/PreToolUse/Stop/PreCompact）・policy 解決器・射影（render）・handoff | Sonnet | ✅ 1a に同梱 |
| **2b** | **部下4名**: chef / housekeeper / steward / secretary（定義・スキーマ・CLI・試験・README） | Sonnet ×4 並列 | ✅ 2026-09-02（試験 chef31・house32・steward33・sec60） |
| **2c** | qa / auditor の定義を v2 向けに移植 | Sonnet | ✅ 1b に同梱 |
| **3** | v1 の `QUEUE.md`/`PROJECTS.md` **取り込み**（`manor import-v1`。v1 のパーサをコピーして使う）→ グラフ検査で重複・待ち解除の候補を数え、レポートの根拠にする | Sonnet | ✅ 2026-09-02（実データ 185 件・辺 303・unresolved 0） |
| **4** | **検分**（qa）→ 執事が裁定・修正 → **レポート2本** → 最終検証（pytest 全緑・`git check-ignore`・射影拒否の実機確認） | qa → 執事 → Sonnet | ✅ 2026-09-02（QA 指摘4件→2件修正・2件は既に反映済み。試験 319 件緑） |

**やらないこと（今回）**: Web ダッシュボード（v1 の butler-board 相当。18k 行。**別段で**）／ Slack・Notion・カレンダーの実接続（④ 環境依存。口だけ用意）／ 振る舞いの回帰試験（v1 設計 `振る舞いの回帰試験.md` を継承候補として残す）／ 音声・アバター。

## 4. 成功の測り方（レポートに載せる数字）

1. **常時ロード層のバイト数**: v1 CLAUDE.md 12.4KB → v2 の実測
2. **構造的に消える不整合**: v1 が挙げた8類型それぞれに、**それを再現しようとして失敗する試験**が1本ずつある
3. **散文 → 機構**: 憲法から消した規則と、それを守る機構（hook / CLI / 試験）の対応表
4. **グラフの問い合わせ**: v1 実データの取り込みで「重複候補」「ブロッカーが片付いたのに待っている」を**件数**で出す（名前は出さない）
5. **委譲の型**: handoff が生成する項目が、v1 AGENTS.md「委譲時に必ず渡すもの」9点を全て含むこと

## 5. 現在地（更新すること）

- 2026-09-02: 段0 完了。段1a・1b を Sonnet へ委譲中。
- 2026-09-02: 段1b 完了（憲法の移植・骨格）。段1a（core）は Sonnet が実装中。
- 2026-09-02: 段1a 完了（core・試験98件）。段2b（部下4名）と段3（取り込み）を並列で委譲中。
- 2026-09-02: 段2b 完了（部下4名）。裁定は ADR-002 §9。段3（取り込み）を待って検分へ。
- 2026-09-02: 段3 完了（取り込み・グラフ問い合わせ）。レポート 01/02 の初版を作成。QA 検分中（段4）。
- 2026-09-02: 段4 完了。QA の指摘（射影保護が MANOR_HOME を解決していない／pre-commit が未配線）を修正し、試験 319 件緑・1 skip。**G1・G2 とも完了。** 次の一手は ROADMAP §3「やらないこと」と レポート01 §6（C10・振る舞いの回帰試験・ダッシュボード・通知）。
- 2026-09-02: 第2期（§6）開始。5a〜5d を並列で委譲。§7 に将来方針を記録。
- 2026-09-05: 第5期は 5a〜5c・5e・5f・5j 完了。
- 2026-09-05: 5d（カレンダー）と 5h-1（画面の多言語）完了。
- 2026-09-05: 5g（総点検）・5h-2（CLI の多言語）・5i（英語 README）完了。**第5期は完了。**

## 6. 第2期（2026-09-02 主人の追加要望）

| 段 | 何を | 誰が | 状態 |
|:--:|------|------|------|
| **5a** | **ダッシュボード**（v1 butler-board 相当を DB の上に）: 要対応／AIの進行中／計画（タイムライン）／記録＋**家**（部下のデータ）。`manor board`。姿の小窓 `/face` | Sonnet | ✅ 2026-09-02（試験38件。執事がブラウザで5画面を本番データで確認） |
| **5b** | **声かけ** `manor notify`（要対応が増えたときだけ・深夜は黙る・初回は黙る。v1 と同じ文面）＋ Stop hook から呼ぶ。声の実体は `home/config.toml` で差し替え（既定は Windows 標準の音声。v1 の VOICEVOX を指してもよい） | Sonnet | ✅ 2026-09-02（`manor notify`・Stop hook から自動。試験31件。Windows 既定音声で実鳴動を確認） |
| **5c** | **自然言語 → DB の振る舞い試験** `tests/behavior/`（`claude -p` を一時 home で回し、DB の副作用と hook の拒否を機械で判定。v1 設計『振る舞いの回帰試験』の実装） | Sonnet → 執事 | ✅ 2026-09-02（10シナリオ。初回 12/14 → 落ちた2本を機構で直し、追試で S4 2/2・S6 3/3 PASS。費用 約 $2.5） |
| **5d** | **v1 実データで DB を作り、齟齬検査**（`manor import-v1 --reconcile`。行ごとに状態・担当・レベル・PJ を突き合わせ） → `home/` に本番 DB を作る | Sonnet → 執事 | ✅ 2026-09-02（`home/manor.db` を作成。task 185・辺 303。**reconcile 1,873 項目一致・齟齬 0**。check 通過） |
| **5e** | **担当との直接対話**の起動口（`claude --agent chef` 等）を文書化・道具化 | 執事 | ✅ 2026-09-02（`manor talk <name>`＝`claude --agent`。CLAUDE.md に「担当として起動されたとき」の節。`docs/talk.md`） |
| **5f** | 主人によるダッシュボードの確認 | 主人 | ✅ 2026-09-02 差し戻し → 5g/5i/5j で修正 → 主人「よくなりました」「よいと思います」（同日） |
| **5g** | **ダッシュボードの機能同等化**（v1 README §2 ＋ app.js を1行ずつ突き合わせ `docs/board_parity.md`、欠落を全部直す。**既定ポートを 8788 に**（v1 の 8787 と共存。v1 の show-face が 8787 の board を入れ替えるため） | Sonnet → 執事 | ✅ 2026-09-02（43項目中 42 ○・対象外 1。原因: `id="panel-running"` 欠落と CSS の nowrap。試験 432 件。執事がブラウザで再確認） |
| **5h** | **v1 との併用中の追従** `manor import-v1 --sync`（v1 由来で manor が触っていない行だけ v1 に合わせる。取り込み後に v1 側が動いた B189/B190 を実データで検出） | Sonnet → 執事 | ✅ 2026-09-02（試験14件。実データで B189 を追従、reconcile 齟齬 0） |
| **5i** | **主人の追加要望（board 確認後）**: ステータス別で主人の作業を最上段・完了を畳む／文脈モーダルの Markdown 描画／プロジェクト別を「関心順」（X 系は最下部、P 系は直近の期日→進行中→最終更新）／**夜勤の仕組みの移植**（`manor night`。登録はしない）／ダミーの要対応 1 件 | Sonnet ×2 → 執事 | ✅ 2026-09-02（試験 531 件。執事がブラウザで確認: 主人の作業が最上段・ツリー p3→p1→p7 と根拠・Markdown 描画・夜勤タブ・要対応 D1） |
| **5j** | **主人の2回目の確認後**: 一言なしの承認・却下／入力中のフォーカス保持／ツリーの `[pj]` 除去。夜勤の時刻依存試験を時計固定に。JS の id 参照を試験で検算 | Sonnet → 執事 | ✅ 2026-09-02（試験 552 件。執事が実測: 11 秒入力してもフォーカス維持） |

**やらないこと**: Slack・Notion・カレンダーの実接続（v1 と競合）。通話（小窓で話しかける）は声かけの次。

## 7. その先（主人の方針・2026-09-02 共有。**具体は未決。方針だけ**）

1. **ダッシュボードを「家庭用 Web アプリ」へ昇格させる。** タスク管理や仕事だけでなく、
   **家庭のルール**を置く／**買い物リスト**／**家計簿アプリとの連携（データ取り込み）**など、
   **機能群として拡張できる**形にする。**全部を0から作らない**——既存の家計簿アプリは使いやすいものが
   大量にあるので、**データを取り込んで連携する**（steward の `steward_expense` へ CSV 取り込み、など）。
   → 5a の board は最初から「画面＝機能モジュールの集合」として作る（部下ごとに1画面、API は `/api/<staff>/...`）
2. **窓口を執事だけにしない。** 主人 → 執事 → 担当 の経路に加えて、**主人 → 担当（料理長など）の直接対話**の
   経路を持つ。起動方法は 5e（`claude --agent <name>`。担当の定義がそのまま人格・道具・モデルになる）
3. **スマホから自宅の Web アプリへ**（Tailscale など。**公開はしない**）。board は `--host` で
   tailnet の IP にだけ待ち受ける。認証は「本人の端末しか届かない」ことで代える（v1 意見箱 I7 の線）。
   常時起動（サービス化）と、スマホ幅の画面は 5a の次
- 2026-09-02: 第2期 5a〜5e 完了。振る舞い試験の落ちた2本（S4 依存と待ち／S6 HG の起票）を機構3つ＋憲法1行で直し、追試で全部 PASS。**残るは 5f（主人によるダッシュボードの確認）。** board は `uv run manor board --open`。
- 2026-09-02: 主人の差し戻し（board の切り替え・計画表）→ v1 を正に 43 項目突き合わせて全部 ○（5g）。既定ポート 8788。v1 併用中の追従 `--sync`（5h）。**5f（主人の確認）待ち。**
- 2026-09-02: 主人の追加要望（5i）完了。夜勤の仕組みは `manor night`（登録は主人の合図で）。要対応のダミー D1/T1 を積んだ。**5f（主人の確認）待ち。**
- 2026-09-02: 5j 完了。要対応のダミー D2（T2）を再テスト用に積んだ。**5f（主人の最終確認）待ち。**

## 8. 第3期（2026-09-02 主人の関心事①〜⑤）

主人の判断（同日）: 夜勤の manor 側登録は**主人の合図まで忘れてよい**／「主人の作業」ブロックは**今の形のまま**。

| 段 | 何を | 誰が | 状態 |
|:--:|------|------|------|
| **6a** | **① データの流れの文書と可視化**: `docs/dataflow.md`（MD ではなく DB が正本であること・更新のパイプライン・読み書きの経路・hooks・取り込み・夜勤）＋ archify で図3枚（構成図・更新のデータフロー・裁定のシーケンス）を `docs/diagrams/` に | Sonnet → 執事 | ✅ 2026-09-03（`docs/dataflow.md`＋図3枚。archify の showcase 検査 9/9・visual-check pass） |
| **6b** | **② 部下にできることの一覧**: README「機能一覧」に執事と部下の能力・CLI を表で。詳細は `docs/staff/` と `docs/reports/02` | Sonnet（6f と同じ担当） | ✅ 2026-09-02（README「できること」の表） |
| **6c** | **③ 家庭用 Web アプリの設計**: `docs/design/ADR-004_web_app.md`（FastAPI ＋ Vite/TS/React、画面＝モジュール、`rules`・`imports` を新設、認証は passcode、PWA） | 執事 | ✅ 2026-09-02 |
| **7a〜7e** | ③の実装: 殻とレジストリ → 部下のモジュール → rules/imports → 認証・PWA・常時起動 → 同等性表で切り替え | Sonnet → 執事 | 未着手（6a・6f の後） |
| **6d** | **④ スマホから**: 7d に含める（`--host` を tailnet の IP に・passcode・`manor web install`）。Tailscale の導入は主人の手 | 主人＋執事 | ✅ 2026-09-03（Tailscale 導入・`tailscale serve` で HTTPS・`require_passcode`。執事が tailnet の URL で 200 と passcode モードを確認） |
| **6f** | **⑤ GitHub で誰でも使える形**: `manor init --demo`（合成データ）・README 全面（導入・使い方・機能一覧・画面）・LICENSE・`git init` の手順・CI（pytest） | Sonnet → 執事 | ✅ 2026-09-03（`init --demo`・README・LICENSE=MIT・`git init`＋3 コミット・CI 定義。**GitHub への push はリモート追加＝human_gate。主人の手で**） |

**順番の考え方**: まず「今あるもの」を正しく説明できる状態にする（6a・6b・6f）。次に Web アプリの殻（7a）で
同等性を確かめてから、家庭の機能（rules・imports）を足す。認証と外出先からの接続（7d・6d）は
**家族のデータを扱う画面が増えてから**にする（増える前に外へ開けない）。
- 2026-09-02: 第3期（§8）開始。ADR-004（Web アプリ化の方針）を執事が起草。6a（データフロー文書＋archify）と 6b/6f（README・demo）を委譲。
- 2026-09-03: 6a（dataflow.md＋archify 図3枚）・6b（README 機能一覧）・6f の一部（`init --demo`・README）完了。試験 560 件。**残: LICENSE の選択（主人）→ git init、7a〜（Web アプリ化）。**

## 9. 第3期・実装（2026-09-03 主人「完成まで進めて OK」）

| 段 | 何を | 誰が | 状態 |
|:--:|------|------|------|
| **7-契約** | `docs/design/ADR-005_api_v1.md`（API v1・認証・画面モジュール・rules・imports の契約） | 執事 | ✅ 2026-09-03 |
| **7-BE** | バックエンド `src/manor/web/`（API v1・passcode 認証・`manor web serve/build/install`）＋ core の `rule` ＋ steward の CSV 取り込み | Sonnet | ✅ 2026-09-03（試験 668 件。`manor web serve`（8789）・`manor rule`・`manor money import`） |
| **7-FE** | フロントエンド `web/`（Vite+TS+React。殻・レジストリ・9 モジュール・PWA） | Sonnet | ✅ 2026-09-03（vitest 47 件・ビルド 1.3MB・モックで 16 画面を確認。実 API との結合は 7-QA） |
| **7-QA** | 結合（BE×FE）・同等性表（`docs/board_parity.md`）の再検分・主人の確認 | qa → 執事 → 主人 | ✅ 2026-09-03（結合で 12 件のずれを修正。同等性 41○/2△/0×。執事が本番 home で確認） |
| **7e** | 切り替え（`manor board` を別名に）／**7f** `git init`・CI・LICENSE（主人） | 執事 | ✅ 2026-09-03（`manor board`＝Web アプリ（8788）。`git init`・最初のコミット 255 ファイル・CI 定義。LICENSE は MIT（主人の選択 2026-09-03）） |
- 2026-09-03: 第3期の実装完了。Web アプリ（9 モジュール）を本番 home で確認、`manor board` を別名に、`git init`＋最初のコミット。**残: LICENSE（主人）・Tailscale の導入（主人の手）・家計簿 CSV の実物での列名確認。**
- 2026-09-03: LICENSE=MIT。Tailscale 導入（主人）→ `docs/tailscale.md` と `[web] require_passcode` を追加。**残: 主人が passcode を設定して `tailscale serve --bg 8789`／家計簿 CSV（保留）／夜勤の登録（合図待ち）。**
- 2026-09-03: 現在地＝**第1〜3期の段はすべて完了**。残っているのは主人の合図待ちと保留のみ: 夜勤の登録（合図待ち）／家計簿 CSV の実物（保留）／GitHub のリモート追加と push（human_gate）。掃除候補: 素 JS の旧 board の削除、`.claude/agents/*.md` に `manor rule list --tag` を足す。

## 10. 第4期（2026-09-03 外部レビューへの応答。ADR-006）

| 段 | 何を | 誰が | 状態 |
|:--:|------|------|------|
| **8-A** | 承認の証跡（`decision.evidence`・`task_event.authorized_by`）／`run` 表と `manor run stats`（夜勤・振る舞い試験が記録）／部下の表の隔離を authorizer で機構化＋静的検算 | Sonnet | ✅ 2026-09-03（試験 +53。authorizer は実行時＋静的の二重） |
| **8-B** | 振る舞い試験を関門に（`manor gate`＋pre-commit）／追記ファイルの寿命（`manor archive`＋C10） | Sonnet | ✅ 2026-09-03（試験 +30。pre-commit から `manor gate --staged`） |
| **8-C** | Web: 要対応カードに証跡、設定に「稼働と費用」 | Sonnet | ✅ 2026-09-03（vitest 52・pytest web 86） |
| **8-D** | 結合・配線・検分・コミット | 執事 | ✅ 2026-09-03（`gate`・`archive` を配線。本番 DB を移行。全体 758 件緑（1 件は時間依存で揺れる: `test_night` の kill 判定。要堅牢化）。Web を再ビルド） |
- 2026-09-03: 第4期（外部レビュー 5 点）完了。次: Remote Control「AI執事 vs monor 比較」へ再レビュー依頼（主人の指示。セッションが一覧に無いときは主人に開いてもらう）。追跡: `tests/test_night.py` の kill 判定の時間依存を固定する。
- 2026-09-03: 再レビュー受領（5点は閉じた。`manor archive` の記録消失を指摘）。D19–D23 を裁定し実装完了（全体 783 件緑）。本番の LOG.md・CHANGELOG.md・GROWTH.md は保持期間内で移す月なし。追跡: 定例（/brief 等）を直接叩く振る舞いシナリオ S11。
- 2026-09-03: 3周目レビュー: 記録消失は閉じた。残り: archive の追記の継ぎ目（改行）／done の authorized_by を decision に限定（C5=C12）→ 完了（cbccd72 の次のコミット。全体 792 件緑）。追跡: `_backfill_authorized_by` の `LIMIT 1` を「done 直前の裁定」に直してから次の import-v1。
- 2026-09-03: 主人の依頼で初回セットアップ（決まった型で呼び名・用途・最初のプロジェクトとタスクを入れる画面）を ADR-007 として設計。`profile` 表＋射影 PROFILE.md＋起動時の1行注入／API `/api/v1/setup`／Web の `setup` モジュール（5段）／CLI `manor profile`・`manor setup --answers`。実装完了。合成 home で 5 段を実機で通し、DB・射影・起動時の1行・check を確認（pytest 833・vitest 60）。行動クラス `general` を追加。
- 2026-09-03: セットアップを用途分岐に改修中（ADR-007 §6。聞きすぎない・全段「あとで」・推奨既定・プレースホルダー）。実装完了。合成 home で「全部あとで」「料理＋家計」の 2 経路を実機で通し、DB（profile・chef_taste）と check を確認（pytest 846・vitest 65）。
- 2026-09-04: 主人が v1 を畳んで manor へ移る意思。ADR-008 で棚卸しと順番を決めた（①小窓の修復と担当ごとの姿 ②声の機構 `manor voice` ③外部連携 Slack→Notion→カレンダー ④小窓で話しかける）。**制約「v1 と競合するので外部連携はやらない」は失効。** 第1期（小窓）に着手。

## 11. 第5期（2026-09-04 主人の方針）— 「拡張機能」と「フォルダを触らせない」

v1 の引き継ぎ（ADR-008）の途中で、主人から**製品としての形**についての方針が出た。以降の実装はこれに従う。

### 11-1 方針（主人の言葉）

1. **Slack・Notion 等の外部連携は「拡張機能」として扱い、コア機能に含めない。** VOICEVOX も外部依存なので同じ位置づけ。Tailscale も同様。Web アプリのサイドバー最下部から**導入状況**を確認でき、必要に応じて有効化・設定手順の表示・API キーの登録ができるようにする（VS Code の拡張機能に相当）
2. **インターフェースは基本的に Web アプリ経由**とし、フォルダの中をユーザーが操作しなくてよいようにする。コマンドプロンプトでのコマンド実行も、VRM ファイルの手置きも要らない状態にする（VRM は画面からアップロード）
3. **セットアップ時にデスクトップへショートカット**を作る（Web アプリの起ち上げ）
4. **多言語対応。** せめて日本語／英語を切り替えられるように。README の英語版も作る（**これは GitHub へ上げる直前に最後に行う**）

### 11-2 段取り

| # | 何 | 依存 | 状態 |
|---|---|---|---|
| **5a** | 拡張機能の枠組み（登録簿・状態の判定・設定の保存・秘密の置き場・Web の「拡張機能」画面）。既存の VOICEVOX / Tailscale をこの枠に載せ替える | ADR-009 | ✅ 2026-09-04 |
| **5b** | Slack 拡張（送信＝ブリーフィング／受信＝返信を裁定として取り込む）。**5a の最初の実物** | 5a | ✅ 2026-09-04 |
| **5c** | Notion 拡張（日記） | 5b | ✅ 2026-09-04（二重投函は現地・手元の両方で防ぐ） |
| **5d** | カレンダー拡張（ICS の読み取り） | 5c | ✅ 2026-09-05（ADR-012 §2。秘書の予定表へ冪等に取り込む） |
| **5e** | VRM を画面からアップロード（`home/face/<agent>.vrm` へ置く口。差し替え・削除も画面から） | — | ✅ 2026-09-04 |
| **5f** | デスクトップのショートカット作成（初回セットアップの最後の段。`manor web serve` を起ち上げてブラウザを開く） | — | ✅ 2026-09-04（黒い窓・絵・起動待ちの修正は 2026-09-05） |
| **5g** | 「フォルダを触らせない」の総点検——CLI でしかできないことを洗い出し、画面から届くようにする | 5e・5f | ✅ 2026-09-05（ADR-013。書き込み59件を洗い、届かない3件を実装） |
| **5h-1** | 多言語対応・**画面**（ja/en の切り替え。訳語 641・抜けを塞ぐ検算3種） | — | ✅ 2026-09-05（ADR-012 §3） |
| **5h-2** | 多言語対応・**CLI**（`manor` の出力と `--help`） | 5h-1 | ✅ 2026-09-05（訳語 689） |
| **5i** | **README の英語版**（GitHub へ上げる直前。最後） | 5h | ✅ 2026-09-05（`README.en.md`） |
| **5j** | **小窓で話しかける（通話）** — v1 `face_talk.py` の移植。押したときだけ LLM を呼ぶ唯一の口。歯止め（1日20往復・01:00〜07:30・会話は残さない）は機構で持つ | ADR-008 §8 | ✅ 2026-09-04（道具は使える。実機で窓からタスク起票を確認） |

**第5期は完了**（2026-09-05）。5a〜5j すべて。

残っているのは、期の外に出した宿題が1つ:

- **passcode の置き場**。いま `home/config.toml` に平文で入っており、ADR-009 で決めた
  秘密の置き場（`~/.manor/secrets/`）と食い違っている。git には入らないので漏れる経路は
  無いが、自分で決めた方針との不整合。**資格情報の引っ越しは主人を締め出す危険がある**ので、
  独立した回に移行を冪等にして行う（ADR-013 §3）

ADR-011（ダッシュボード・担当一覧・小窓）と担当ごとの声・名前と絵は、この表の外で主人の
指摘に応じて足したもの。5a〜5c・5e・5f・5j は完了。

### 11-3 この方針が既存の設計に与える影響

- **「コア」と「拡張」の線引きが要る。** コア＝manor だけで完結するもの（タスク・部下・Web アプリ・小窓・声かけ〈OS 既定の声〉・夜勤・関門）。拡張＝外部の何か（アプリ・サービス・鍵）が要るもの。**拡張が1つも入っていなくても manor は完全に動く**、を不変条件にする
- `manor notify` の声は**コア**（OS 既定）、VOICEVOX は**拡張**。この線は ADR-008 D5 の「VOICEVOX を依存にしない」と同じ判断の延長
- 外部送信は行動クラス上 HG 固定。**拡張になっても承認は省かない**（ADR-008 D1）
- 2026-09-04: 主人の指摘で 5j（通話）を追加。小窓がタブで開く件と「小文字に揃えます」の崩れも修正中。
