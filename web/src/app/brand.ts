/* manor web — 表示名（主人に見える名前）を1か所で決める。
 *
 * **内部の識別子とは別物である。** コマンド名（`manor …`）・localStorage の鍵
 * （`manor-web.theme`）・API の経路はすべて `manor` のまま——主人の環境に既に
 * 書き込まれているので、名前を変えると動いているものが壊れる。ここで決めるのは
 * **画面に出る名前**だけ。バックエンドの対（`src/manor/branding.py`）と揃える。
 *
 * 主人の指定（2026-09-05）: 「名前は "AI Manor" とします」。
 */

/** 画面に出す名前。ここだけ書き換えれば全体に効く。 */
export const APP_NAME = "AI Manor";
